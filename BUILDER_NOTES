To list all the files created in the Debian package:
dpkg-deb -c build/flower-6.1.2-x86_64.deb

To merge develop branch back onto main after changes:
git checkout main
git pull origin main
git merge develop
git push origin main
